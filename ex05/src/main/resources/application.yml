spring:

  shell:
    interactive:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    password: ''
    username: sa
    url: jdbc:h2:file:./h2/hw

  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
